import 'package:flutter/material.dart';

class ProfileEditingController extends StatelessWidget {
  const ProfileEditingController({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
